# This is Git's per-user configuration file.
[user]
	name = Dan Jones
	email = djdan@amazon.com
[core]
	excludesfile = /Users/deltajuliet/.gitignore_global
[include]
        path = /Users/deltajuliet/.gitconfig.aliases
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[credential]
	helper = !aws --profile djdan codecommit credential-helper $@
